---
lane: done
---

# WP19 - Phase 2: Cross-WP Validation Skill

| Field | Value |
|-------|-------|
| Spec | `.sdd/specs/003-planner-v2.spec.md` |
| Priority | P1 |
| Lane | for_review |
| Depends on | WP14, WP15, WP16, WP17, WP18 |
| Goal | Implement the plan-cross-wp-validation skill that audits all WPs and contracts for consistency, generates a shared config schema, fixes inconsistencies, and verifies 100% spec artifact coverage |
| Status | Complete |
| Independent Test | Dispatch plan-cross-wp-validation against a plan accumulator with known inconsistencies. Verify: inconsistencies are fixed, config-schema file is generated in contracts/shared/, README includes Consistency Notes, no orphan FRs remain |
| Parallelisable | No (must run after all other Phase 2 skills) |
| Prompt | `.sdd/plans/WP19-phase2-cross-wp-validation.md` |

## Objective

Implement the final Phase 2 skill: `plan-cross-wp-validation`. This skill is the capstone of the planning pipeline -- it reads ALL plan files and ALL contract files generated by earlier skills, performs a 7-point consistency audit (data contracts, API/interface contracts, dependencies, configuration, test requirements, spec traceability, contract-to-task alignment), generates a shared configuration schema, fixes any inconsistencies, and documents corrections.

## Spec References

FR-051, FR-052, FR-053, FR-054, Section 4.10, Section 7.2

## Tasks

### T19-01 - Implement plan-cross-wp-validation SKILL.md structure

- **Description**: Replace the stub `plan-cross-wp-validation/SKILL.md` with the full skill implementation. Define purpose, input contract (Phase 2, all plan files + all contract files + spec + artifacts), execution sequence, and output format.
- **Spec refs**: FR-051, FR-023, FR-024
- **Parallel**: No
- **Acceptance criteria**:
  - [x] SKILL.md follows the common plan-skill contract with phase=2 (FR-023)
  - [x] Skill reads ALL plan files and ALL contract files as input (FR-051)
  - [x] Skill reads the spec and companion artifacts as input (FR-054)
  - [x] Skill references `.github/skills/PLAN-SKILL-CONTRACT.md` for the common contract
- **Test requirements**: none
- **Depends on**: none
- **Implementation Guidance**:
  - This skill is the only Phase 2 skill that reads ALL WPs (others scope per-WP)
  - Input is the full plan accumulator plus all previously generated contract files

### T19-02 - Implement data contract consistency check

- **Description**: Write instructions for checking that entity fields (names, types, validation rules) are identical across all WPs that reference the same entity. Flag and fix mismatches.
- **Spec refs**: FR-051.1
- **Parallel**: Yes (with T19-03, T19-04, T19-05, T19-06, T19-07)
- **Acceptance criteria**:
  - [x] The skill SHALL verify entity fields match across WPs (FR-051.1)
  - [x] If WP-A defines field `userId: string` and WP-B defines `userId: number`, the skill SHALL flag it and fix to the canonical type from the spec data model
  - [x] Shared data formats (dates, IDs, enums) SHALL be consistent across all WPs
  - [x] Fixes are applied directly to the contract files, not just reported
- **Test requirements**: BDD (US-04 Scenario 1)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - Canonical types come from the spec's data model (Section 7)
  - When two WPs disagree, the spec is the tiebreaker

### T19-03 - Implement API/interface contract consistency check

- **Description**: Write instructions for checking that API endpoint definitions and interface signatures match between producer and consumer WPs. Verify method, path, params, return type, and error codes.
- **Spec refs**: FR-051.2
- **Parallel**: Yes (with T19-02, T19-04, T19-05, T19-06, T19-07)
- **Acceptance criteria**:
  - [x] The skill SHALL verify every API endpoint defined in one WP matches its consumers in other WPs (FR-051.2)
  - [x] The skill SHALL check: HTTP method, path, parameters, return type, error response schemas
  - [x] If WP-A defines `GET /api/users` returning `User[]` and WP-B expects `UserResponse[]`, the skill SHALL flag and fix
  - [x] Interface contracts defined in one WP and implemented in another SHALL be identical
- **Test requirements**: BDD (US-04 Scenario 2)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - Compare api-contracts files across WP contract directories
  - Also check interface-contracts files for cross-WP references

### T19-04 - Implement dependency integrity check

- **Description**: Write instructions for verifying the dependency graph: no circular dependencies, all `Depends on` references are valid WP IDs, parallelisable WPs have no shared mutable state.
- **Spec refs**: FR-051.3
- **Parallel**: Yes (with T19-02, T19-03, T19-05, T19-06, T19-07)
- **Acceptance criteria**:
  - [x] The skill SHALL verify no circular dependencies exist (FR-051.3)
  - [x] The skill SHALL verify all `Depends on` WP IDs are valid (FR-051.3)
  - [x] The skill SHALL verify WPs marked `Parallelisable: Yes` have no shared mutable state
  - [x] The dependency graph in README.md SHALL match individual WP declarations
- **Test requirements**: BDD (US-04 Scenario 3)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - Implement topological sort to detect cycles
  - Cross-reference WP metadata headers with README table

### T19-05 - Implement configuration consistency and config schema generation

- **Description**: Write instructions for checking config keys across WPs and generating the shared `config-schema.<ext>` file.
- **Spec refs**: FR-051.4, FR-052
- **Parallel**: Yes (with T19-02, T19-03, T19-04, T19-06, T19-07)
- **Acceptance criteria**:
  - [x] The skill SHALL verify environment variables and config keys use identical names/types across WPs (FR-051.4)
  - [x] Default values SHALL be consistent -- WP-A does not assume a different default than WP-B for the same config key (FR-051.4)
  - [x] The skill SHALL generate `config-schema.<ext>` in `.sdd/plans/contracts/shared/` (FR-052)
  - [x] Config schema SHALL include: name, type, default, validation rule, description for each env var (FR-052)
- **Test requirements**: BDD (US-04 Scenario 1)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - Scan all WP files and contract files for env var references
  - Aggregate into a single config schema with deduplication
  - Output to `contracts/shared/config-schema.<ext>` (not per-WP)

### T19-06 - Implement test consistency and spec traceability checks

- **Description**: Write instructions for verifying coverage requirements are consistent across WPs and every FR is assigned to exactly one task.
- **Spec refs**: FR-051.5, FR-051.6
- **Parallel**: Yes (with T19-02, T19-03, T19-04, T19-05, T19-07)
- **Acceptance criteria**:
  - [x] The skill SHALL verify coverage requirements (80% code, 90% branch) are stated consistently in all WPs that reference testing (FR-051.5)
  - [x] The skill SHALL verify every FR in the spec's traceability matrix is assigned to exactly one task across all WPs (FR-051.6)
  - [x] No orphan FRs (FR in spec but not in any WP task)
  - [x] No duplicate FR assignments (same FR in multiple WP tasks without explicit justification)
- **Test requirements**: BDD (US-04 Scenario 1)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - Parse traceability matrix from spec to get full FR list
  - Parse all task `Spec refs` fields from all WPs to build assignment map
  - Report orphans and duplicates

### T19-07 - Implement contract-to-task alignment check

- **Description**: Write instructions for verifying every generated contract file is referenced by at least one task in a WP.
- **Spec refs**: FR-051.7
- **Parallel**: Yes (with T19-02, T19-03, T19-04, T19-05, T19-06)
- **Acceptance criteria**:
  - [x] The skill SHALL verify every contract file in `.sdd/plans/contracts/` is referenced by at least one task (FR-051.7)
  - [x] Orphan contract files (generated but not referenced by any task) SHALL be flagged
  - [x] Missing contract references (task references a contract that does not exist) SHALL be flagged
- **Test requirements**: BDD (US-04 Scenario 1)
- **Depends on**: T19-01
- **Implementation Guidance**:
  - List all files under `contracts/` directories
  - Search all task descriptions and implementation guidance for references to those files
  - Report orphans and missing references

### T19-08 - Implement inconsistency fix and documentation

- **Description**: Write instructions for the skill to fix inconsistencies directly in WP and contract files, and document all corrections in the plan README under "Consistency Notes".
- **Spec refs**: FR-053
- **Parallel**: No
- **Acceptance criteria**:
  - [x] The skill SHALL fix inconsistencies directly in WP and contract files (FR-053)
  - [x] The skill SHALL document all corrections in README under "Consistency Notes" (FR-053)
  - [x] Each correction entry SHALL include: what was inconsistent, which files were affected, what was changed
- **Test requirements**: BDD (BDD Scenario 4 from US-04)
- **Depends on**: T19-02, T19-03, T19-04, T19-05, T19-06, T19-07
- **Implementation Guidance**:
  - Corrections are applied in-place, not as a separate report
  - README Consistency Notes section is appended, not overwritten

### T19-09 - Implement 100% spec artifact coverage verification

- **Description**: Write instructions for verifying that generated contract files collectively cover all entities, endpoints, error codes, and state machines from the spec's companion artifacts.
- **Spec refs**: FR-054
- **Parallel**: No
- **Acceptance criteria**:
  - [x] The skill SHALL verify 100% coverage of spec companion artifacts (FR-054)
  - [x] No spec artifact entity may be missing from the plan's contracts
  - [x] No spec artifact endpoint may be missing from the plan's contracts
  - [x] No spec artifact error code may be missing from the plan's contracts
  - [x] No spec artifact state machine may be missing from the plan's contracts
- **Test requirements**: BDD (US-02 Scenario 1, US-04 Scenario 1)
- **Depends on**: T19-08
- **Implementation Guidance**:
  - Compare `.sdd/specs/artifacts/<NNN>-<idea-name>/` contents against `.sdd/plans/contracts/` contents
  - Generate a coverage matrix showing which spec artifacts are covered by which contract files

### T19-10 - Verify encoding compliance

- **Description**: Run automated Unicode check on the SKILL.md file. Fix any violations.
- **Spec refs**: Section 9.2
- **Parallel**: No
- **Acceptance criteria**:
  - [x] Zero prohibited Unicode characters in SKILL.md
  - [x] All hyphens are ASCII `-`, all quotes are straight
  - [x] Python validation script confirms clean results
- **Test requirements**: unit (encoding validation)
- **Depends on**: T19-09
- **Implementation Guidance**:
  - Reuse the Python encoding check from WP14

## Implementation Notes

- This is the only skill that depends on ALL other plan skills having produced their output
- It reads both the plan files (.sdd/plans/WP*.md) and the contract files (.sdd/plans/contracts/**)
- The config schema is the only output that goes to `contracts/shared/` (all other contracts are per-WP)
- This skill naturally runs last in the Phase 2 sequence

## Parallel Opportunities

- T19-02 through T19-07 (the six consistency checks) can all run in parallel since they read the same inputs independently
- T19-08 (fix + document) and T19-09 (100% coverage) must run after all checks complete

## Risks & Mitigations

- **Risk**: Large specs generate many contract files, making cross-WP validation slow. **Mitigation**: The skill operates within a single context window and uses structured comparison (not line-by-line diffing).
- **Risk**: Auto-fix may introduce new inconsistencies. **Mitigation**: Consistency notes document every change; the Coder can review during implementation.

## Activity Log

- 2026-04-05T00:00:00Z - planner - lane=planned - Work package created
- 2026-04-05T15:00:00Z - coder - lane=doing - Starting implementation
- 2026-04-05T15:30:00Z - coder - lane=for_review - All tasks complete, submitted for review
- 2026-04-05T16:00:00Z - reviewer - lane=done - Review PASS, no blocking findings

## Review

**Verdict**: PASS
**Reviewed**: 2026-04-05T16:00:00Z
**Reviewer**: review-spec (manual dispatch)

### Findings Summary

| Category | Count |
|----------|-------|
| PASS | 11 |
| WARN | 1 |
| FAIL | 0 |
| N/A | 0 |

### Findings

**SPEC-001 [PASS]** - FR-051 (7-point consistency audit)
- All 7 checks are implemented in Step 2 with correct scope, examples, and fix instructions
- Check 1: data contract consistency (field names, types, validation, relationships)
- Check 2: API/interface consistency (method, path, params, return type, errors)
- Check 3: dependency integrity (topological sort, valid IDs, parallel state check)
- Check 4: configuration consistency (env var names, types, defaults)
- Check 5: test consistency (coverage thresholds, BDD scenarios)
- Check 6: spec traceability (FR->task mapping, orphans, duplicates)
- Check 7: contract-to-task alignment (orphan/missing contracts)

**SPEC-002 [PASS]** - FR-052 (config schema generation)
- Step 3 generates `config-schema.<ext>` in `contracts/shared/`
- Manifest header uses correct `Generated by: plan-cross-wp-validation skill`
- Schema includes name, type, default, validation, description per entry
- Both TypeScript and Python examples provided

**SPEC-003 [PASS]** - FR-053 (inconsistency fix + documentation)
- Step 4 fixes inconsistencies using `[CONSISTENCY FIX]` marker per FR-027
- Step 5 documents corrections in README "Consistency Notes" section
- Corrections table includes: category, files affected, what changed
- Handles clean-report case (no inconsistencies found)

**SPEC-004 [PASS]** - FR-054 (100% spec artifact coverage)
- Step 6 verifies entities, endpoints, error codes, state machines, interfaces
- Coverage matrix format with counts and percentages
- Graceful handling when spec artifacts do not exist (prose-only spec)

**SPEC-005 [PASS]** - FR-023 (9-input contract)
- Input Contract table has all 9 inputs matching PLAN-SKILL-CONTRACT.md exactly

**SPEC-006 [PASS]** - FR-024 (4-step execution sequence)
- Steps: Read SKILL.md, Read plan state (broadest scope), Read spec + artifacts, Write fixes + outputs

**SPEC-007 [PASS]** - FR-027 (`[CONSISTENCY FIX]` marker)
- Step 4 explicitly instructs use of the marker when modifying existing files

**SPEC-008 [PASS]** - US-04 Scenario 1 (field name inconsistency)
- Check 1 example shows WP-A `userId: string` vs WP-B `userId: number` detection and fix

**SPEC-009 [PASS]** - US-04 Scenario 2 (circular dependency)
- Check 3 specifies topological sort to detect cycles

**SPEC-010 [PASS]** - US-04 Scenario 3 (clean report)
- Step 5 includes explicit "No inconsistencies found" template

**SPEC-011 [PASS]** - Encoding compliance
- Zero prohibited Unicode characters confirmed by automated check
- 294 lines, well under 800-line limit

**SPEC-012 [WARN]** - FR-052 config schema detail
- Step 3 text requires "Which WP(s) reference it" per config entry, but neither TypeScript nor Python example includes this field. The instruction is present in prose but not demonstrated in code examples. Non-blocking since the prose instruction is clear.
