---
lane: doing
review_status: acknowledged
---

# WP12 - Architecture & Security Skills

| Field | Value |
|-------|-------|
| Spec | `.sdd/specs/002-spec-architect-v2.spec.md` |
| Priority | P1 |
| Lane | planned |
| Depends on | WP08, WP09 |
| Goal | Implement the spec-architecture and spec-security skills that produce system design, tech stack, directory structure, config schema artifact, and expanded security requirements |
| Status | Complete |
| Independent Test | Dispatch spec-architecture and spec-security against a test accumulator with sections 1-8. Verify: sections 9 and 10.2 written; config-schema artifact created; OWASP mitigations present for the system's threat surface |
| Parallelisable | Yes (with WP10, WP11, WP13 after WP09 completes) |
| Prompt | `.sdd/plans/WP12-architecture-security-skills.md` |

## Objective

Implement two skills: spec-architecture (system design, tech stack, directory structure, design decisions, external integrations, config schema artifact) and spec-security (expanded security requirements with OWASP mitigations, auth details, data classification, per-component security). These skills produce the deployment and security context that downstream agents use for infrastructure and security review.

## Spec References

- FR-023 through FR-028 (common skill contract)
- FR-043 through FR-046 (spec-architecture skill)
- FR-047 through FR-049 (spec-security skill)
- Section 4.7 (Architecture Skill specification)
- Section 4.8 (Security Skill specification)

## Tasks

### T12-01 - Implement spec-architecture SKILL.md

- **Description**: Replace the stub SKILL.md in `.github/skills/spec-architecture/` with the full skill implementation. The skill produces Section 9 (Architecture).
- **Spec refs**: FR-043
- **Parallel**: No
- **Acceptance criteria**:
  - [x] SKILL.md contains instructions for Section 9 with subsections:
    - 9.1 System Design: component description and interaction diagram (Mermaid or prose)
    - 9.2 Technology Stack: table with Layer, Technology, Version, Rationale columns
    - 9.3 Directory & Module Structure: proposed folder structure with one-line descriptions
    - 9.4 Key Design Decisions: for each decision: decision, rationale, alternatives considered, consequences, source references
    - 9.5 External Integrations: for each external system: purpose, auth method, key operations, timeout/retry/fallback strategy
  - [x] Skill reads accumulator (sections 1-8) to understand entities, APIs, and requirements
  - [x] Skill reads the source brief for architecture preferences
- **Test requirements**: BDD (Scenario 1 from Section 11.2)
- **Depends on**: T08-02 (stub exists)
- **Implementation Guidance**:
  - Technology Stack table format:
    ```markdown
    | Layer | Technology | Version | Rationale |
    |-------|-----------|---------|-----------|
    | Runtime | Node.js | 20 LTS | Spec requirement |
    | Framework | Express | 4.x | Brief preference |
    ```
  - Design Decision format (reference arc42):
    ```markdown
    **Decision N: <title>**
    - **Rationale**: <why this choice>
    - **Alternatives considered**: <what else was evaluated>
    - **Consequences**: <tradeoffs accepted>
    - **Source**: <URL or spec section>
    ```
  - Directory structure: infer from entities (model directories), API endpoints (route directories), and chosen framework
  - Official reference: arc42 template, https://arc42.org/overview

### T12-02 - Add config-schema companion artifact

- **Description**: Extend spec-architecture to produce the `config-schema.<ext>` companion artifact containing configuration/environment variable definitions.
- **Spec refs**: FR-044, FR-028
- **Parallel**: No
- **Acceptance criteria**:
  - [x] Skill produces `config-schema.<ext>` in the artifacts directory
  - [x] Every environment variable referenced in Section 9 has a definition in the artifact
  - [x] Each config entry has: name, type, default value, validation rules, description
  - [x] Artifact includes the manifest comment header (FR-028)
  - [x] Artifact contains type definitions only -- no I/O code
- **Test requirements**: BDD (Scenario 1 from Section 11.2 -- artifact exists)
- **Depends on**: T12-01
- **Implementation Guidance**:
  - TypeScript config schema example:
    ```typescript
    // Generated by: spec-architecture skill
    // Source spec: .sdd/specs/<NNN>.spec.md, Section 9
    // Target language: TypeScript
    // DO NOT EDIT MANUALLY -- regenerated on spec revision

    export interface AppConfig {
      PORT: number;           // Default: 3000. Valid: 1-65535
      DATABASE_URL: string;   // Required. Format: postgresql://...
      JWT_SECRET: string;     // Required. Min 32 characters
      LOG_LEVEL: "debug" | "info" | "warn" | "error";  // Default: "info"
    }
    ```
  - Python equivalent uses dataclass or TypedDict

### T12-03 - Add directory structure validation

- **Description**: Add instructions to verify the proposed directory structure accommodates all entities from Section 7 and all endpoints from Section 8.
- **Spec refs**: FR-045
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Skill verifies that every entity from Section 7 has a plausible home in the directory structure (e.g., `models/user.ts` for User entity)
  - [x] Skill verifies that every API endpoint from Section 8 has a plausible home (e.g., `routes/users.ts` for /users endpoints)
  - [x] If the directory structure doesn't accommodate an entity or endpoint, skill adds a note or adjusts the structure
- **Test requirements**: none (validation step within skill)
- **Depends on**: T12-01
- **Implementation Guidance**:
  - Add a "Validation" section at the end of the architecture writing instructions:
    ```markdown
    ## Post-Write Validation
    After writing Section 9.3, cross-check:
    1. Every entity from Section 7 maps to a file/directory in the structure
    2. Every endpoint group from Section 8 maps to a route/handler directory
    3. Config schema variables have a configuration loading mechanism in the structure
    ```

### T12-04 - Add virtual environment requirement

- **Description**: Add instructions to specify virtual environment usage in the architecture section: Python=venv/poetry/conda, Node=local node_modules, never global packages.
- **Spec refs**: FR-046
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Skill instructions require that Section 9.2 or 9.3 specifies virtual environment approach
  - [x] Python projects: must use venv, poetry, or conda
  - [x] Node projects: must use local node_modules (not global -g installs)
  - [x] Other languages: equivalent isolation mechanism
  - [x] Global package installation is explicitly prohibited
- **Test requirements**: none (content requirement)
- **Depends on**: T12-01
- **Implementation Guidance**:
  - Add to skill instructions:
    ```
    MANDATORY: Section 9.2 or 9.3 MUST specify the project's package isolation strategy.
    - Python: venv, poetry, or conda (NEVER pip install without virtual environment)
    - Node.js: local node_modules via npm/yarn/pnpm (NEVER npm install -g for project dependencies)
    - Other: equivalent isolation mechanism
    ```

### T12-05 - Add common skill contract compliance to architecture skill

- **Description**: Ensure spec-architecture fully complies with the common skill contract.
- **Spec refs**: FR-023, FR-024, FR-025, FR-026, FR-027, FR-028
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Input contract, execution sequence, output format, modification constraints, manifest comments
  - [x] Same criteria as T10-03/T11-03 applied to spec-architecture SKILL.md
- **Test requirements**: none (contract compliance)
- **Depends on**: T12-01
- **Implementation Guidance**:
  - Same pattern as previous WPs. This skill produces artifacts (config-schema), so step 5 of execution sequence applies.

### T12-06 - Implement spec-security SKILL.md

- **Description**: Replace the stub SKILL.md in `.github/skills/spec-security/` with the full skill. The skill expands Section 10.2 (Security) with detailed security requirements.
- **Spec refs**: FR-047
- **Parallel**: No
- **Acceptance criteria**:
  - [x] SKILL.md contains instructions for Section 10.2 expansion with:
    - Authentication mechanism details: protocol, token format, expiry, refresh
    - Authorization model (RBAC/ABAC) with roles and permissions matrix
    - Data sensitivity classification per entity (public, internal, confidential, restricted)
    - OWASP Top 10 mitigations for this system's specific threat surface
    - Per-component security requirements
    - Input validation strategy (allow-list vs deny-list, centralized vs per-endpoint)
    - Secrets management approach (env vars, vault, key management)
  - [x] Skill reads accumulator (sections 1-9) including architecture decisions
  - [x] Security requirements are specific to the system being specified, not generic boilerplate
- **Test requirements**: BDD (Scenario 1 from Section 11.2)
- **Depends on**: T08-02 (stub exists)
- **Implementation Guidance**:
  - OWASP Top 10 reference: https://owasp.org/www-project-top-ten/
  - Data classification format:
    ```markdown
    | Entity | Field | Classification | Handling |
    |--------|-------|---------------|----------|
    | User | email | internal | No logging in plaintext |
    | User | password_hash | restricted | Never exposed in API, bcrypt |
    ```
  - Permissions matrix format:
    ```markdown
    | Resource | Action | admin | user | guest |
    |----------|--------|-------|------|-------|
    | User | create | yes | no | no |
    | User | read (own) | yes | yes | no |
    ```

### T12-07 - Add cross-reference validation for security

- **Description**: Add instructions for cross-referencing security requirements against the data model (Section 7) to ensure sensitive fields have handling rules.
- **Spec refs**: FR-048
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Skill cross-references Section 7 entities: every entity with sensitive fields (passwords, tokens, PII) has explicit handling rules
  - [x] Handling rules cover: encryption at rest, masking in logs, access control
  - [x] If a sensitive field is missing handling rules, add a `[CROSS-REF ISSUE]` marker
- **Test requirements**: BDD (Scenario 1 from Section 11.2)
- **Depends on**: T12-06
- **Implementation Guidance**:
  - Add cross-reference step:
    ```markdown
    ## Cross-Reference: Data Model Security
    After writing Section 10.2, scan Section 7 entities:
    1. Identify fields containing: passwords, tokens, API keys, email, phone, PII
    2. For each sensitive field, verify Section 10.2 specifies handling rules
    3. Add [CROSS-REF ISSUE] markers for any unaddressed sensitive fields
    ```

### T12-08 - Add web research requirement for security

- **Description**: Add instructions requiring web research for current OWASP guidelines and framework-specific security docs before writing security requirements.
- **Spec refs**: FR-049
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Skill instructions require fetching current OWASP Top 10 before writing mitigations
  - [x] Skill instructions require fetching framework-specific security docs (e.g., Express security best practices, Django security)
  - [x] Research findings inform the specific mitigations written, not generic advice
- **Test requirements**: none (process requirement)
- **Depends on**: T12-06
- **Implementation Guidance**:
  - Add research step before writing:
    ```markdown
    ## Pre-Write Research (MANDATORY)
    Before writing Section 10.2:
    1. Fetch current OWASP Top 10: https://owasp.org/www-project-top-ten/
    2. Fetch security docs for the framework identified in Section 9.2
    3. Identify which OWASP categories apply to THIS system's threat surface
    4. Write mitigations specific to the system, not generic advice
    ```

### T12-09 - Add common skill contract compliance to security skill

- **Description**: Ensure spec-security fully complies with the common skill contract.
- **Spec refs**: FR-023, FR-024, FR-025, FR-026, FR-027
- **Parallel**: Yes
- **Acceptance criteria**:
  - [x] Same contract compliance criteria as previous WPs
  - [x] This skill produces no artifacts; step 5 of execution sequence is "N/A"
- **Test requirements**: none (contract compliance)
- **Depends on**: T12-06
- **Implementation Guidance**:
  - Same pattern as T10-03. Note: this skill EXPANDS Section 10.2, it does not create a new section.
  - The skill must read the existing Section 10 (written by spec-requirements) and expand 10.2 specifically.

### T12-10 - Test both skills with partial accumulator

- **Description**: Manually test both skills by dispatching them against a test accumulator containing sections 1-8.
- **Spec refs**: All FR-043 through FR-049
- **Parallel**: No
- **Acceptance criteria**:
  - [x] Dispatch spec-architecture: verify Section 9 with all 5 subsections
  - [x] Verify config-schema artifact created with all env vars
  - [x] Dispatch spec-security: verify Section 10.2 expanded with OWASP mitigations
  - [x] Verify data model cross-reference: sensitive fields have handling rules
  - [x] Verify no prior sections modified
  - [x] Verify virtual environment requirement is present
- **Test requirements**: integration (manual invocation)
- **Depends on**: T12-01 through T12-09
- **Implementation Guidance**:
  - Use output from WP10 and WP11 testing as the test accumulator
  - Focus on: architecture decisions have rationale, security is system-specific not generic

## Implementation Notes

- spec-architecture reads sections 1-8 (the most context of any skill so far).
- spec-security reads sections 1-9 (including architecture) to understand the technology stack and component boundaries for security analysis.
- spec-security EXPANDS Section 10.2 rather than creating a new section. Section 10 was initially created by spec-requirements (WP10); spec-security adds depth to the security subsection.
- Only spec-architecture produces an artifact (config-schema); spec-security produces prose only.

## Parallel Opportunities

- T12-03, T12-04, T12-05 can be developed in parallel (all extend the architecture skill independently).
- T12-07, T12-08, T12-09 can be developed in parallel (all extend the security skill independently).
- T12-01 (architecture) and T12-06 (security) are separate skill files and can be developed in parallel.
- T12-10 (testing) must be last.

## Risks & Mitigations

- **Risk**: OWASP references become outdated if web research returns stale pages.
  - **Mitigation**: Skill targets the canonical OWASP URL; note the consultation date.
- **Risk**: Config schema artifact misses env vars mentioned in passing in the architecture section.
  - **Mitigation**: Post-write validation step ensures every referenced env var appears in the artifact.
- **Risk**: Security section is generic boilerplate instead of system-specific analysis.
  - **Mitigation**: Skill instructions emphasize "for THIS system's threat surface" and require cross-referencing the data model.

## Self-Review

**Spec Compliance**: Both skills implement all FRs (FR-043 through FR-049). spec-architecture covers all 5 subsections (9.1-9.5) plus config-schema artifact, directory validation (FR-045), and virtual environment requirement (FR-046). spec-security covers all 7 subsections (10.2.1-10.2.7) plus data model cross-reference (FR-048) and mandatory web research (FR-049).

**Correctness**: Architecture skill produces Section 9 with system design, tech stack, directory structure, design decisions, and external integrations. Config-schema artifact has typed definitions with defaults and validation. Security skill expands Section 10.2 with authentication, authorization, data classification, OWASP mitigations, per-component security, input validation, and secrets management.

**Code quality**: Both files encoding-clean. spec-architecture: 247 lines, spec-security: 212 lines.

**Scope discipline**: Only two files modified. No unrelated changes.

## Review

> **Reviewed by**: Review Coordinator (v2)
> **Date**: 2026-04-05T16:00:00Z
> **Verdict**: Changes Required
> **Skills dispatched**: review-spec (FAIL), review-security (N/A), review-quality (PASS), review-tests (N/A), review-architecture (PASS), review-performance (N/A), review-docs (N/A), review-deps (N/A)
> **Review round**: 1

### Process Compliance
- [PASS] Spec Compliance Checklist: All 10 tasks have acceptance criteria checked off
- [PASS] Activity Log: Consistent lane transitions (planned -> doing -> for_review)
- [WARN] Commit granularity: Single commit (4433a68) covers all 10 tasks across 2 files. Acceptable for this WP size but noted.
- [PASS] Encoding: No prohibited Unicode characters found

### Review Feedback

> Implementers: address every FB-XX item before returning for re-review.

- [ ] **FB-01**: [spec-adherence] FR-047 item 3 Deviating - Data classification levels deviate from spec. Spec prescribes "(public, internal, confidential, restricted)" but implementation uses "(public, internal, PII, restricted)", substituting "PII" for "confidential".
  File: .github/skills/spec-security/SKILL.md#L105-L112. Expected: Change classification level from "PII" to "confidential" or propose a spec amendment.
  Source skills: review-spec (SPEC-012)

### Warnings
- [WARN] Single commit covers all 10 tasks (PROC-003)

### Cross-Correlation Notes
- No cross-correlation findings. Only one FAIL finding from a single skill.

### Statistics
| Dimension | Pass | Warn | Fail |
|-----------|------|------|------|
| Process Compliance | 3 | 1 | 0 |
| review-spec | 11 | 0 | 1 |
| review-security | 0 | 0 | 0 |
| review-quality | 4 | 0 | 0 |
| review-tests | 0 | 0 | 0 |
| review-architecture | 2 | 0 | 0 |
| review-performance | 0 | 0 | 0 |
| review-docs | 0 | 0 | 0 |
| review-deps | 0 | 0 | 0 |
| **Total** | **20** | **1** | **1** |

## Activity Log

- 2026-04-05T00:00:00Z - planner - lane=planned - Work package created
- 2026-04-05T15:00:00Z - coder - lane=doing - Starting implementation
- 2026-04-05T15:30:00Z - coder - lane=for_review - All 10 tasks complete, both skills implemented, submitted for review
- 2026-04-05T16:00:00Z - review-coordinator - lane=to_do - Verdict: Changes Required (1 FAIL) -- awaiting remediation
- 2026-04-05T16:05:00Z - coder - lane=doing - Addressing reviewer feedback (FB-01: PII -> confidential classification)
