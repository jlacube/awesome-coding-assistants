# Generated by: spec-architecture skill
# Source spec: .sdd/specs/010-sdd-pipeline-hardening.spec.md, Section 9
# Target language: YAML
# DO NOT EDIT MANUALLY -- regenerated on spec revision

# Configuration Schema for SDD Pipeline Hardening
#
# This pipeline has no runtime environment variables or app config.
# "Configuration" consists of the registry files and default values
# that agents read at startup.

pipeline_config:
  enum_registry:
    path: ".github/schemas/enums.yaml"
    required: true
    description: "Central enum registry. Must exist for agents to start."

  base_handoff_schema:
    path: ".github/schemas/base-handoff.schema.yaml"
    required: false
    description: "Shared validation patterns. Optional; agents fall back to inline rules."

  handoff_schemas_dir:
    path: ".github/schemas/"
    required: true
    description: "Directory containing all handoff schemas."
    min_files: 11
    comment: "8 existing + 3 new return schemas"

  patterns_files:
    paths:
      - ".sdd/reviews/spec-patterns.md"
      - ".sdd/reviews/plan-patterns.md"
      - ".sdd/reviews/code-patterns.md"
      - ".sdd/reviews/doc-patterns.md"
    required: false
    description: "Domain patterns files. Optional; agents proceed without patterns if missing."

# Default Values
defaults:
  review_cycles:
    type: integer
    value: 0
    description: "Default review cycle count for WPs without the field"

  docs_completed:
    type: boolean
    value: false
    description: "Default documentation status for WPs without the field"

  coverage_code:
    type: integer
    value: 80
    minimum: 0
    maximum: 100
    description: "Default minimum code coverage percentage"

  coverage_branch:
    type: integer
    value: 90
    minimum: 0
    maximum: 100
    description: "Default minimum branch coverage percentage"

  patterns_version:
    type: integer
    value: 1
    minimum: 1
    description: "Initial patterns_version for new pattern files"

# File Naming Conventions
naming_conventions:
  agent_files: "{name}.agent.md"
  skill_files: "{skill-name}/SKILL.md"
  skill_contracts: "{DOMAIN}-SKILL-CONTRACT.md"
  handoff_schemas: "{source}-to-{target}.schema.yaml"
  base_schema: "base-handoff.schema.yaml"
  enum_registry: "enums.yaml"
  wp_files: "WP{NN}-{slug}.md"
  spec_files: "{NNN}-{name}.spec.md"
  pattern_files: "{domain}-patterns.md"
