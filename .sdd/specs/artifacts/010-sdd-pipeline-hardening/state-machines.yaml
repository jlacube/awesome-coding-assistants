# Generated by: spec-data-model skill
# Source spec: .sdd/specs/010-sdd-pipeline-hardening.spec.md, Section 7
# Target language: YAML
# DO NOT EDIT MANUALLY -- regenerated on spec revision

# WP Lane State Machine - Section 7.1
wp_lane:
  states: [planned, doing, for_review, to_do, done, blocked]

  transitions:
    - from: planned
      to: doing
      guard: Coder selects WP; all dependencies have lane=done
      side_effects:
        - Coder appends Activity Log entry

    - from: doing
      to: for_review
      guard: All tasks complete, tests passing, coverage met
      side_effects:
        - Coder appends Activity Log entry

    - from: for_review
      to: done
      guard: Reviewer verdict is approved
      side_effects:
        - Reviewer appends Activity Log entry

    - from: for_review
      to: to_do
      guard: Reviewer verdict is changes_required
      side_effects:
        - Reviewer increments review_cycles by 1
        - Reviewer appends Activity Log entry

    - from: to_do
      to: doing
      guard: Coder acknowledges feedback
      side_effects:
        - Coder sets review_status=acknowledged
        - Coder appends Activity Log entry

    - from: "*"
      to: blocked
      guard: Unresolvable blocker identified
      side_effects:
        - Agent appends Activity Log entry

    - from: blocked
      to: planned
      guard: Blocker resolved
      side_effects:
        - Appends Activity Log entry

  invalid_transition_error: "Invalid lane transition from '{from}' to '{to}'. See state machine definition."

# Spec Status State Machine (reference from enums)
spec_status:
  states: [Draft, Validated, Approved]

  transitions:
    - from: Draft
      to: Validated
      guard: Spec Architect completes specification and user approves
      side_effects:
        - Spec file Status field updated

    - from: Validated
      to: Approved
      guard: Planner has decomposed spec into WPs
      side_effects:
        - Spec file Status field updated

  invalid_transition_error: "Invalid spec_status transition from '{from}' to '{to}'. Valid: Draft->Validated->Approved."

# Pipeline Stage State Machine (reference from Orchestrator)
pipeline_stage:
  states: [idle, ideation, specification, planning, implementation, review, documentation, complete]

  transitions:
    - from: idle
      to: ideation
      guard: User provides an idea description

    - from: idle
      to: specification
      guard: Existing brief found without a spec

    - from: idle
      to: planning
      guard: Existing spec found without WPs

    - from: idle
      to: implementation
      guard: Existing WPs found with lane=planned

    - from: ideation
      to: specification
      guard: Ideation agent completes brief

    - from: specification
      to: planning
      guard: Spec status set to Validated

    - from: planning
      to: implementation
      guard: Planner produces WP files

    - from: implementation
      to: review
      guard: Coder sets WP lane=for_review

    - from: review
      to: documentation
      guard: Review Coordinator sets WP lane=done

    - from: review
      to: implementation
      guard: Review Coordinator sets WP lane=to_do

    - from: documentation
      to: implementation
      guard: Docs Agent completes and next WP exists

    - from: documentation
      to: complete
      guard: Docs Agent completes and no WPs remain

    - from: implementation
      to: complete
      guard: All WPs lane=done and documented

  invalid_transition_error: "Invalid pipeline_stage transition from '{from}' to '{to}'. See valid transitions table."

# Verdict-to-Lane Mapping
verdict_lane_mapping:
  approved: done
  changes_required: to_do
  validation: "updated_lane SHALL match verdict: approved->done, changes_required->to_do"
