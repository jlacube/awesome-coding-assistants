# Generated by: spec-api-design skill
# Source spec: .sdd/specs/010-sdd-pipeline-hardening.spec.md, Section 8
# Target language: YAML
# DO NOT EDIT MANUALLY -- regenerated on spec revision

# Error Catalog
# Each error code maps to the interface section where it is defined,
# the condition that triggers it, and the agent response behavior.

errors:
  # 8.1 WP Frontmatter Interface Errors
  E-001:
    key: WP_FILE_NOT_FOUND
    interface: "8.1 WP Frontmatter"
    condition: "WP file does not exist at specified path"
    response: "Halt"
    message_template: "WP file not found at {path}"
    source_frs: [FR-001, FR-003, FR-005, FR-006]

  E-002:
    key: INVALID_FRONTMATTER
    interface: "8.1 WP Frontmatter"
    condition: "YAML frontmatter cannot be parsed"
    response: "Halt"
    message_template: "Invalid YAML frontmatter in {path}"
    source_frs: [FR-001, FR-003]

  E-003:
    key: INVALID_FIELD_TYPE
    interface: "8.1 WP Frontmatter"
    condition: "Field value has unexpected type (e.g., string for integer field)"
    response: "Log warning, use default value"
    message_template: "Field '{field}' in {path} has type '{actual_type}', expected '{expected_type}'. Using default: {default}"
    source_frs: [FR-001, FR-003, FR-007]

  E-004:
    key: COVERAGE_OUT_OF_RANGE
    interface: "8.1 WP Frontmatter"
    condition: "coverage_code or coverage_branch not in [0, 100]"
    response: "Halt"
    message_template: "Invalid {field} value '{value}'. Must be an integer 0-100."
    source_frs: [FR-034, FR-035]

  E-005:
    key: INVALID_LANE_VALUE
    interface: "8.1 WP Frontmatter"
    condition: "lane value not in enum registry"
    response: "Halt"
    message_template: "Invalid lane value '{value}'. Valid values: planned, doing, for_review, to_do, done, blocked."
    source_frs: [FR-010]

  E-006:
    key: FIELD_ABSENT
    interface: "8.1 WP Frontmatter"
    condition: "New field not present in pre-hardening WP file"
    response: "Use default value (no error)"
    message_template: null
    source_frs: [FR-007]

  # 8.2 Enum Registry Interface Errors
  E-010:
    key: ENUM_REGISTRY_NOT_FOUND
    interface: "8.2 Enum Registry"
    condition: "enums.yaml file missing from .github/schemas/"
    response: "Halt"
    message_template: "Enum registry not found at .github/schemas/enums.yaml"
    source_frs: [FR-008]

  E-011:
    key: ENUM_GROUP_NOT_FOUND
    interface: "8.2 Enum Registry"
    condition: "Requested enum group not in file"
    response: "Halt"
    message_template: "Enum group '{name}' not found in enums.yaml"
    source_frs: [FR-009]

  E-012:
    key: INVALID_ENUM_VALUE
    interface: "8.2 Enum Registry"
    condition: "Value not in specified enum group"
    response: "Halt"
    message_template: "Invalid {group} value '{value}'. Valid values: {values}"
    source_frs: [FR-010, FR-011, FR-012, FR-013]

  E-013:
    key: ENUM_PARSE_ERROR
    interface: "8.2 Enum Registry"
    condition: "enums.yaml is malformed YAML"
    response: "Halt"
    message_template: "Failed to parse enums.yaml: {parse_error}"
    source_frs: [FR-008]

  # 8.3 Handoff Schema Interface Errors
  E-020:
    key: SCHEMA_NOT_FOUND
    interface: "8.3 Handoff Schema"
    condition: "Schema file missing"
    response: "Halt"
    message_template: "Schema file not found at {path}"
    source_frs: [FR-021, FR-023, FR-025]

  E-021:
    key: MISSING_CONTEXT_FIELD
    interface: "8.3 Handoff Schema"
    condition: "Required context field absent from handoff prompt"
    response: "Halt"
    message_template: "Missing required field '{name}' in {schema_name} handoff"
    source_frs: [FR-022, FR-024, FR-026]

  E-022:
    key: INVALID_FIELD_VALUE
    interface: "8.3 Handoff Schema"
    condition: "Field value does not match expected"
    response: "Halt"
    message_template: "Field '{field}': expected '{expected}', got '{actual}'"
    source_frs: [FR-022, FR-024, FR-026]

  E-023:
    key: ARTIFACT_NOT_FOUND
    interface: "8.3 Handoff Schema"
    condition: "Required artifact file missing"
    response: "Halt"
    message_template: "Required artifact not found at {path}"
    source_frs: [FR-027]

  E-024:
    key: BASE_SCHEMA_NOT_FOUND
    interface: "8.3 Handoff Schema"
    condition: "base-handoff.schema.yaml missing"
    response: "Warning; fall back to inline rules"
    message_template: "Base schema not found at .github/schemas/base-handoff.schema.yaml. Using inline validation rules."
    source_frs: [FR-049, FR-051]

  E-025:
    key: SCHEMA_PARSE_ERROR
    interface: "8.3 Handoff Schema"
    condition: "Schema file is malformed YAML"
    response: "Halt"
    message_template: "Failed to parse schema {path}: {parse_error}"
    source_frs: [FR-027]

  # 8.4 Pattern File Interface Errors
  E-030:
    key: PATTERNS_FILE_NOT_FOUND
    interface: "8.4 Pattern File"
    condition: "Patterns file does not exist"
    response: "Set patterns to 'No active patterns'; log warning"
    message_template: "{file} not found, proceeding without patterns."
    source_frs: [FR-052]

  E-031:
    key: PATTERNS_UNREADABLE
    interface: "8.4 Pattern File"
    condition: "File exists but cannot be read on re-check"
    response: "Use last cached patterns; log warning"
    message_template: "Cannot read {file}. Using cached patterns from version {last_version}."
    source_frs: [FR-054]

  E-032:
    key: PATTERNS_VERSION_INVALID
    interface: "8.4 Pattern File"
    condition: "patterns_version is not an integer"
    response: "Treat as 0 (always reload)"
    message_template: "patterns_version in {file} is not an integer. Treating as 0."
    source_frs: [FR-052]

  # 8.5 Activity Log Interface Errors
  E-040:
    key: ACTIVITY_LOG_SECTION_MISSING
    interface: "8.5 Activity Log"
    condition: "WP file has no Activity Log section"
    response: "Create the section heading, then append entry"
    message_template: null
    source_frs: [FR-016]

  E-041:
    key: WP_FILE_WRITE_FAILURE
    interface: "8.5 Activity Log"
    condition: "Cannot write to WP file"
    response: "Halt (critical-path agents) or log warning (advisory agents)"
    message_template: "Cannot write to WP file at {path}"
    source_frs: [FR-016, FR-017, FR-018, FR-019]

  # 8.6 Dependency Graph Interface Errors
  E-050:
    key: CIRCULAR_DEPENDENCY
    interface: "8.6 Dependency Graph"
    condition: "Cycle detected in dependency graph"
    response: "Halt"
    message_template: "Circular dependency detected: {cycle}"
    source_frs: [FR-042]

  E-051:
    key: MISSING_DEPENDENCY
    interface: "8.6 Dependency Graph"
    condition: "depends_on references non-existent WP"
    response: "Halt"
    message_template: "{wp} depends on {dep} which does not exist"
    source_frs: [FR-042]

  E-052:
    key: ALL_WPS_BLOCKED
    interface: "8.6 Dependency Graph"
    condition: "No WPs eligible (all have unmet dependencies)"
    response: "Report blocked status"
    message_template: "No WPs are ready. Blocked WPs: {list}"
    source_frs: [FR-040]
